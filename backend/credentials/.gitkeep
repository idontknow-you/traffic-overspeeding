# This file exists only so the empty credentials/ folder is kept in git.
# Do NOT put your actual serviceAccountKey.json content here —
# it's gitignored on purpose. Drop your real key file in this folder
# as serviceAccountKey.json; git will silently ignore it.
